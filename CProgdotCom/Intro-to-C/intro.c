//Introduction to C
#include <stdio.h>

//main function
int main()
{
    printf("Happy programmers write happy code.\n");
    getchar();
    //getchar() reads in a single character and waits for the user to hit enter before reading the character.
    //It keeps that window from closing because the program is not done yet because it waits for you to hit enter.
    //Including getchar() gives you time to see the program run
    return 0;
}
