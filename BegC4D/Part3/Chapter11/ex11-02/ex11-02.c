#include <stdio.h>

/**
 * main - Uses a for loop for iteration
 *
 * Return: Always 0 (Success)
 * Author: IanoNjuguna
 */
int main(void)
{
	int count = 0;

	while (count < 10)
	{
		puts("Get off my lawn, you kids.");
		count++;
	}
	return (0);
}

