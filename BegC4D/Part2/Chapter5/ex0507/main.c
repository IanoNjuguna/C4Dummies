#include <stdio.h>
#include <stdlib.h>
/**
 * main -Entry point.
 * Description: Using the puts() and printf() functions
 * Return: Always 0 (Success)
 * Author: IanoNjuguna
 */

int main(void)
{
	puts("The result is:");
	printf("%d\n", 0 + 50 * 1 - 60 - 60 * 0 + 10);
	return (0);
}
