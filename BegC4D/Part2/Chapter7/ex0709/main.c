#include <stdio.h>
/**
 * main - Entry point.
 * Description: Working with character variables using the printf() function
 * Return: Always 0 (Success)
 * Author: IanoNjuguna
 */

int main(void)
{
	char a, b, c, d;

	a = 'W';
	b = 'o';
	c = 'w';
	d = '\n';

	putchar (a);
	putchar (b);
	putchar (c);
	putchar (d);
	return (0);
}
