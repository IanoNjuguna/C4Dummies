#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 * Author: IanoNjuguna
 */

int main(void)
{
	int count;

	for (count = -5; count < 6; count++)
	{
		printf("%d\n", count);
	}
	return (0);
}

