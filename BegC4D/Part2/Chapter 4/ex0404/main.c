#include <stdio.h>

/**
 * main - Entry point.
 * Description: Program that prompts the standard output
 *		to display multiple lines of text.
 * Return: Always 0 (Success)
 * Author: IanoNjuguna
 */

int main(void)
{
	puts("Hickory, dickory, dock,");
	puts("The mouse ran up the clock.");
	puts("The clock struck one,");
	puts("The mouse ran down the dock,");
	puts("Hickory, dickory, dock.");
	return (0);
}
