#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 * Author: IanoNjuguna
 */

int main(void)
{
	int a, b;

	a = 5;
	b = -3;

	if (a == b)
	{
		printf("%d equals %d\n", a, b);
	}
	else
	{
		printf("%d not equals %d\n", a, b);
	}
	return (0);
}

