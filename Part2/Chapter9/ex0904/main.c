#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 * Author: IanoNjuguna
 */

int main(void)
{
	int count;

	for (count = 11; count <= 19; count++)
	{
		printf("%d\t", count);
	}
	printf("\n");

	return (0);
}

