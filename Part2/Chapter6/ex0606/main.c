#include <stdio.h>
#include <stdlib.h>
/**
 * main -Entry point.
 * Description: working with various data types
 * Return: Always 0 (Success)
 * Author: IanoNjuguna
 */

int main(void)
{
	int GLORKUS;

	GLORKUS = 22;

	printf("The value of GLORKUS is %d\n", GLORKUS);
	printf("The value of GLORKUS plus 16 is %d\n", GLORKUS + 16);
	printf("The value of GLORKUS times itself is %d\n", GLORKUS * GLORKUS);
	return (0);
}
